[general]
component_name = htmldiff

[version]
major = 1a
func = 1
minor = 0
snap_type_progression = dev,beta,rc
snap_type = dev
snap_number = 1

[file_locations]
rpm_spec = SPECS/lrms_htmldiff.spec
setup_py = setup.py
setup_cfg = setup.cfg

[svn_location]
branches_base = branches
release_branches = %(branches_base)s/release
tags_base = tags
release_tags = %(tags_base)s/release
final_tags = %(release_tags)s/final
snap_tags = %(release_tags)s/snapshot

